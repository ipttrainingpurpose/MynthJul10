# Copy this file to ".env" and fill in real values. Never commit ".env" to source control.

BASE_URL=https://demowebshop.tricentis.com

# Demo Web Shop test account (this is a public training/demo site, not production)
LOGIN_EMAIL=sureshbabu7780@gmail.com
LOGIN_PASSWORD=Ayyammal@1980

# Browser: chromium | firefox | webkit
BROWSER=chromium

# true = run headless, false = show the browser window
HEADLESS=true

# Default per-action slow-motion delay in ms (helps with demo/debugging, set to 0 for CI)
SLOW_MO=0
